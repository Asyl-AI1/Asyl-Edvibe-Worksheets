{
  "name": "asyl-edvibe-worksheets",
  "private": true,
  "scripts": {
    "bootstrap": "npm --workspace frontend install && npm --workspace backend install",
    "dev": "concurrently \"npm --workspace backend run dev\" \"npm --workspace frontend run dev\"",
    "start": "concurrently \"npm --workspace backend start\" \"npm --workspace frontend start\"",
    "build": "npm --workspace backend run build && npm --workspace frontend run build",
    "seed": "npm --workspace backend run seed"
  },
  "workspaces": [
    "frontend",
    "backend"
  ],
  "devDependencies": {
    "concurrently": "^8.0.1"
  }
}